export default class User{
    constructor(id,firtsName, lastName, city){
        this.firtsName = firtsName
        this.id = id 
        this.city = city
        this.lastName = lastName
        
        console.log("User oluştu")
    }
}